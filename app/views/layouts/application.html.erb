<!DOCTYPE html>
<html>
<head>
  <title>Park Road Books</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>


<div class="navbar">
  <div class="navbar-default">
    <div class="container">
      <a class="brand" href="/"> <span class="glyphicon glyphicon-book"></span> <span>Park Road Books</span></a>
      <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <% if user_signed_in? %>
            <span class="icon-bar">
              <%= link_to "Social Accounts", authentications_path %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
            </span>
        <% else %>
            <span class="icon-bar">
              <%= link_to "Sign In", new_user_session_path %>
            </span>
            <span class="icon-bar">
              <%= link_to "Sign Up", new_user_registration_path %>
            </span>
        <% end %>
      </a>
      <!-- Everything in here gets hidden at 940px or less -->
      <div class="nav-collapse">
        <!-- menu items gets rendered here instead -->
      </div>
    </div>
  </div>
</div>

<div class="flash">
       <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
</div>

<div class="container">
<%= yield %>

</body>
</html>
